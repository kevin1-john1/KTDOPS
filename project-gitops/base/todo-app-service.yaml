apiVersion: v1
kind: Service
metadata:
  name: todo-app-svc
spec:
  type: LoadBalancer
  selector:
    app: todo-app
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000